{
  "name": "projectseniorservice",
  "version": "2.0.0",
  "private": true,
  "description": "Private Family Соня OS on Cloudflare Workers + KV + Telegram Mini App + Admin Panel",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node scripts/check.mjs",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
